---
import { formatDate, type DateStyle } from '../utils/posts';

interface Props {
  date: Date;
  style?: DateStyle;
}

const { date, style = 'full' } = Astro.props;
---

<time datetime={date.toISOString()}>{formatDate(date, style)}</time>
